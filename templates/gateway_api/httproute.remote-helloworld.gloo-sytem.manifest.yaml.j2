---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: remote-helloworld-route
  namespace: {{ gloo_gateway_namespace }}
spec:
  parentRefs:
  - name: {{ ingress_gateway }}
    group: gateway.networking.k8s.io    ### Added for kgateway, does it break istio gateway? Maybe fixed now??
    kind: Gateway                       ### Added
    namespace: {{ ingress_namespace }}  ### Added
  hostnames: ["remote-helloworld.{{ tldn }}"]
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - group: gateway.kgateway.dev
      kind: Backend
      name: remote-helloworld-backend
      namespace: {{ gloo_gateway_namespace }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: remote-tls-helloworld-route
  namespace: {{ gloo_gateway_namespace }}
spec:
  parentRefs:
  - name: {{ ingress_gateway }}
    group: gateway.networking.k8s.io    ### Added for kgateway, does it break istio gateway? Maybe fixed now??
    kind: Gateway                       ### Added
    namespace: {{ ingress_namespace }}  ### Added
  hostnames: ["remote-tls-helloworld.{{ tldn }}"]
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - group: gateway.kgateway.dev
      kind: Backend
      name: remote-tls-helloworld-backend
      namespace: {{ gloo_gateway_namespace }}
...

